{
  "name": "jev-router",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Router-only Jev Choice gate + loop-stop/escalate + shadow permission catalogs + tiny tool catalog + shadow typed calls + Grok Build harness + shadow check. No secrets in this package.",
  "bin": {
    "jev-router": "./jev-router.mjs"
  },
  "scripts": {
    "test": "node test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  }
}
